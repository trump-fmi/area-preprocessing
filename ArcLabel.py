class ArcLabel:

    def __init__(self, text, center, start_angle, end_angle, inner_radius, outer_radius):
        self.text = text
        self.center = center
        self.start_angle = start_angle
        self.end_angle = end_angle
        self.inner_radius = inner_radius
        self.outer_radius = outer_radius
